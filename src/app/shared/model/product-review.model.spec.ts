import { ProductReview.Model } from './product-review.model';

describe('ProductReview.Model', () => {
  it('should create an instance', () => {
    expect(new ProductReview.Model()).toBeTruthy();
  });
});
